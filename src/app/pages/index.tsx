import { JSX } from 'react';

const EbullicionPage: () => JSX.Element = (): JSX.Element => {
    return (
        <div style={{ padding: 40, fontFamily: 'Arial' }}>
            <h1>Calculador de Punto de Ebullición</h1>
        </div>
    );
};

export default EbullicionPage;